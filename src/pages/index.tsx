import type { NextPage } from 'next';
import PageTitle from '@components/common/PageTitle';

const Home: NextPage = () => {
  return (
    <div>
      <PageTitle title="홈" />
    </div>
  );
};

export default Home;
